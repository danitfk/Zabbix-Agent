---
language: python
python: "2.7"

before_install:
  - sudo apt-get update -qq

install:
  - pip install ansible

  - "printf '[defaults]\nroles_path = ../' > ansible.cfg"

script:
  - ansible-playbook -i ../Zabbix-Agent/tests/inventory ../Zabbix-Agent/tests/test.yml --syntax-check
